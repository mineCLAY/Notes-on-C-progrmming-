/*
 * 将整型和字符型混合使用[失败]
 *
 * 11.05 Mixed_Types_Original.c
 */

#include <stdio.h>

int main(void) {

  // 用户的 名字
  puts("Enter you name:");
  char name[32];
  scanf("%s" , name);

  // 用户的 年龄
  puts("Enter you age:");
  int age;
  scanf("%d" , &age);

  // 用户的 身高
  puts("Enter you height(unit cm):");
  double height;
  scanf("%lf" , &height);

  // 用户的 形别
  puts("Enter you sex:");
  char sex;
  scanf("%c" , &sex);

  // 用户的 体重
  puts("Enter you weight(unit kg):");
  double weight;
  scanf("%lf" , &weight);

  // 输出用户的 信息
  printf("%s , %d 岁 , 形别: %c , 身高: %lf , 体重: %lf" ,
        name , age , sex , height , weight);;

  return 0;
}
