01. stdio.h 里定义了输入 (scanf) 和 输出 (printf) 等函数

02. math.h 里定义了各种数学函数 , 如 double sqrt(double x) , double log(double x) , double sin(double x) 等等

03. string.h 里定义各种字符串处理的函数 , 如 strcmp()、strcpy()等等

04. stdlib.h 里定义了 EXIT_SUCCESS 和 EXIT_FAILURE 符号 , 表示成功与失败
