/*
 * 键盘输入你的用户名、密码、确认密码
 * 如果两次密码不一样就提示错误
 *
 * 如果正确就把用户名、密码
 * 存入
 * 以 "用户名" 命名的文件中
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <errno.h>

int mian(void) {

  

  return 0;
}
