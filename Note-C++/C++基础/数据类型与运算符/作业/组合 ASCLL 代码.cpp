/*
 * 将下面的数字按顺序转换成
 * 相应的ASCII码
 * 打印出相应的ASCII码
 * 对应的字母并进行组合
 * 数字如下:
 * 97、110、103、101、108、97、66、97、98、121
 */

#include <iostream>

using namespace std;

int main(void) {

  // 打印
  cout << (char)97 << (char)110 << (char)103 << (char)101
       << (char)108 << (char)97 << (char)98 << (char)97
       << (char)98 << (char)121 << endl;

  return 0;
}
